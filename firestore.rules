rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ⚠️ RÈGLES TEMPORAIRES ULTRA-PERMISSIVES POUR DEBUG
    // À RESTREINDRE APRÈS LES TESTS !
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
